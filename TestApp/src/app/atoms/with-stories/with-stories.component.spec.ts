import { WithStoriesComponent } from './with-stories.component';

describe('WithStoriesComponent', () => {
  let component: WithStoriesComponent;

  beforeEach(() => {
    component = new WithStoriesComponent();
   });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
