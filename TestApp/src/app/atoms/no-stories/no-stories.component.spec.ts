import { NoStoriesComponent } from './no-stories.component';

describe('NoStoriesComponent', () => {
  let component: NoStoriesComponent;

  beforeEach(() => {
    component = new NoStoriesComponent();
   });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
