import { MyShinyAtomComponent } from './my-shiny-atom.component';

describe('MyShinyAtomComponent', () => {
  let component: MyShinyAtomComponent;

  beforeEach(() => {
    component = new MyShinyAtomComponent();
   });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
